<div class="card">
  <% if group %>
    <div class="card__top">
      <div class="card__content multiple_votes_form_group_title">
        <h5 class="card__title"><label><input type="checkbox"> <%= translated_attribute group.title %></label></h5>
      </div>
    </div>
  <% end %>
  <div class="card__content multiple_votes_form">
    <% responses.each do |response| %>
      <div>
        <%= form.check_box :id, id: "vote_id_#{response.id}", name: "responses[#{response.id}]", as: :boolean, label: false %>
        <%= form.label :id, translated_attribute(response.title), for: "vote_id_#{response.id}" %>
      </div>
    <% end %>
  </div>
</div>
